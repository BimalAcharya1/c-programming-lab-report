/*3. WAP to illustrate using (Function with no arguments and no return type). */
#include <stdio.h>
void printMessage() {
    printf("Hello, this is a message from the function!\n");
}

int main() {
    // Call the function
    printMessage();

    return 0;
}

